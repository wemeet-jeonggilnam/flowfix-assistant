spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: flowfix-test-secret-key-must-be-at-least-256-bits-long-here!!

rag-engine:
  base-url: http://localhost:8003
