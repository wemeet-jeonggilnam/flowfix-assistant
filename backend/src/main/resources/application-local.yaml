spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flowfix
    username: flowfix
    password: flowfix
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    show-sql: true

logging:
  level:
    com.wemeet.flowfixassistant: DEBUG
    org.springframework.web: DEBUG

rag-engine:
  base-url: http://localhost:8003
